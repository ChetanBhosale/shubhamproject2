"use client";

/**
 * Centralized icon barrel.
 *
 * Uses Phosphor Icons. Identifiers below are the friendly names used across
 * the app — they map to Phosphor components so a single import change in each
 * file is enough. Default weight is set globally via IconContext in AppShell
 * (so callers don't need to pass a weight prop).
 *
 * Hard rule: no Star/Sparkles/general emoji icons anywhere.
 */
export {
  House as HomeIcon,
  NotePencil as NotebookPen,
  Brain as Bot,
  Brain as Brain,
  Wind as Wind,
  UserCircle as User2,
  List as Menu,
  Moon as Moon,
  Sun as Sun,
  CaretLeft as ChevronLeft,
  CaretRight as ChevronRight,
  Bed as Bed,
  Smiley as Smile,
  Plus as Plus,
  Headphones as Headphones,
  BookOpen as BookOpen,
  PencilSimple as Pencil,
  Trash as Trash2,
  Heart as Heart,
  ChatCircle as MessageCircle,
  Phone as Phone,
  Pause as Pause,
  Play as Play,
  SpeakerHigh as Volume2,
  ArrowsClockwise as RefreshCw,
  Microphone as Mic,
  PaperPlaneTilt as SendHorizonal,
  FloppyDisk as Save,
  X as X,
  Camera as Camera,
  Image as ImageIcon,
  MusicNotes as Music2,
  ArrowRight as ArrowRight,
  ArrowUpRight as ArrowUpRight,
  DotsSixVertical as GripVertical,
  Coffee as Coffee,
  Drop as Droplet,
  Footprints as Footprints,
  Pulse as Activity,
  CreditCard as CreditCard,
  SignOut as LogOut,
  HandHeart as HeartHandshake,
  Bell as Bell,
  Flame as Flame,
  Leaf as Leaf,
  Compass as Compass,
  Lightbulb as Lightbulb,
  ChatCircleDots as ChatCircleDots,
  Target as Target,
  Lifebuoy as LifeBuoy,
  Check as Check,
  Users as Users,
} from "@phosphor-icons/react";
